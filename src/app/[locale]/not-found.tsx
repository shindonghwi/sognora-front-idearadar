import { NotFoundPage } from '@/features/not-found';

/**
 * NotFound Page (404)
 *
 * Next.js 컨벤션 파일 - 실제 UI는 features/not-found에서 관리
 */
export default function NotFound() {
  return <NotFoundPage />;
}
